# root kasutajana:
installi - apt install git
# tavakasutajana:
esmakordselt:

a) Konfigureeri oma kasutaja alt
git config --global user.name "OmaEes Perenimi"
git config --global user.email omameil@khk.ee
git config --global core.editor nano

b) Loo kaust, mille failid vajavad jälgimist
mkdir skriptimine

c) Sisene antud kataloogi
cd skriptimine

d) Tee lokaalne git repo antud kaustas muudatuste jälgimiseks
git init

remote seadistamine:
e) git remote add nimi github:aadress
git remote add skriptimine git@github.com:kasutajanimi/reponimi.git

f) git remote kontroll:
git remote -v

põhikasutamine:

a) pane fail jälgimisele:
git add failinimi
